



<% provide(:title, 'All events') %>
<h1>All events</h1>

<%= will_paginate @events %>


<ul class = "users">
  <% @events.each do |event| %>
     <%= render event %>
  <% end %>
</ul>
  
  <%= will_paginate @events%>
 



