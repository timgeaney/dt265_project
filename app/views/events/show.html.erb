<% provide(:title, @event.title) %>
<div class="row">
  <aside class="span4">
    <section>
      <h1>
        <%= @event.title %>
      </h1>
    </section>
  </aside>
  </aside>
  <div class="span8">
    <h2>
      <%= @event.date %>
      <h2>
  </div>
  <p>
     <%= link_to "Update", edit_event_path(@event) %>
     <%= link_to "Delete", @event, :method => :delete, :confirm => "Are you sure you wanat to delete this event?" %>
  </p>



